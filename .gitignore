tags
test.sh
.luarc.json
nvim

spell/
lazy-lock.json

# gravity.nvim: Manifest overrides (deep merge for dependencies/metadata)
# manifest.overrides.json

# gravity.nvim: Config file overrides (file replacement for configs)
# README.md is tracked to explain usage, but actual override files are ignored
configs.overrides/*
!configs.overrides/README.md

# gravity.nvim: State files (never tracked)
.sync_state.json
backups/
